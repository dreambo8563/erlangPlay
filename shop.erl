-module(shop).
-export([cost/1]).

%% export([funcName/argNum])

cost(oranges)   ->5;
cost(newspaper) ->8;
cost(apples)    ->2;
cost(pears)     ->9;
cost(milk)      ->7;
cost(good)      ->99;
cost(test)      ->9.

